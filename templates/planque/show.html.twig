{% extends 'base.html.twig' %}

{% block title %}Planque{% endblock %}

{% block content %}
    <h1>Planque</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ planque.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ planque.code }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ planque.adresse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_planque_index') }}">Retour à la liste</a>

    <a href="{{ path('app_planque_edit', {'id': planque.id}) }}">Editer</a>

    {{ include('planque/_delete_form.html.twig') }}
{% endblock %}
