<form method="post" action="{{ path('app_statut_delete', {'id': statut.id}) }}" onsubmit="return confirm('Confirmez vous la suppression de cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ statut.id) }}">
    <button class="btn">Delete</button>
</form>
