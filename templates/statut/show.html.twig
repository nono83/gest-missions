{% extends 'base.html.twig' %}

{% block title %}Statut{% endblock %}

{% block content %}
    <h1>Statut</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ statut.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ statut.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_statut_index') }}">Retour à la liste</a>

    <a href="{{ path('app_statut_edit', {'id': statut.id}) }}">Editer</a>

    {{ include('statut/_delete_form.html.twig') }}
{% endblock %}
